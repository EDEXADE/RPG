using Iscronium.Models.Units;

namespace Iscronium.Models.Effects;

/*
def attack_decorator(func):
  def wrapper(self, attack_args):
    # Выполняем исходную функцию атаки
    attack_result = func(self, attack_args)
    
    # Применяем эффекты атаки после стандартной атаки 
    effect = self.calculate_attack_effects(attack_args)
    if effect: 
      attack_result += effect
      
    # Возвращаем итоговый результат
    return attack_result

  return wrapper
*/